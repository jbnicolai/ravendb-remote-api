express = require "express"

describe 'basic setup >>', ->

  #==============================================================================
  # basic test
  #==============================================================================
  it "#indexOf()", ->
    assert.equal(-1, [1,2,3].indexOf(5))